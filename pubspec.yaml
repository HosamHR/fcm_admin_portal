name: fcm_admin_portal
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.4 <4.0.0"

dependencies:
  async: ^2.11.0
  collection: ^1.18.0
  extension_google_sign_in_as_googleapis_auth: ^2.0.12
  flutter:
    sdk: flutter
  geocode: ^1.0.3
  google_sign_in: ^6.2.1
  googleapis: ^12.0.0
  googleapis_auth: ^1.4.1
  http: ^1.2.0
  intl: ^0.19.0
  retry: ^3.1.2
  shared_preferences: ^2.2.2
  signals: ^4.2.2
  signals_flutter: ^4.2.2
  google_sign_in_web: ^0.12.3+2
  go_router: ^13.2.0
  url_strategy: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
